nodes:
  raw_movie:
    type: cala.gui.components.Encoder
    params:
      frame_rate: 30
    depends:
      - frame.value
  prep_movie:
    type: cala.gui.components.Encoder
    params:
      frame_rate: 30
    depends:
      - flatten.frame
#  component_count:
#    type: cala.gui.components.component_counter
#    depends:
#      - index: counter.idx
#      - traces: assets.traces