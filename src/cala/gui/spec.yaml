nodes:
  prep_movie:
    type: cala.gui.components.Encoder
    params:
      frame_rate: 30
    depends:
      - glow.frame
  metric_plot:
    type: cala.gui.components.component_counter
    depends:
      - something.idk  # might want to get from multiple nodes...
